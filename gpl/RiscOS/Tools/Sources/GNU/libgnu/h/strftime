#include <kernel.h>
#include <time.h>

size_t nstrftime (char *s, const size_t maxsize, const char *format, const struct tm * t, int ut, int ns);

size_t ds_strftime (char *s, const size_t maxsize, const char *format, const struct tm * t);
