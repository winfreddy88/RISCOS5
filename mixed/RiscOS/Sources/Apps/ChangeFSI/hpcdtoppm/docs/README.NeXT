

There are at least two ports to the NeXT machine with
graphical user interface. One of them is called "pCD".
 
Since i do not get informed about these ports, i don't
know on which version of hpcdtoppm they are based and
where to get them. 

