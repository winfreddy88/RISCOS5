NAME "test"
ENDHEADER

loop:
  SET counter 10
  GOSUB do_this  
  DEC counter
  IF counter > 0 GOTO loop

  IFN WAIT "test",400 GOTO something_different
  SEND "Ok."
  GOTO the_end
  something_different:
  SEND "Not ok."

  the_end:

  END

  do_this:
  SEND "blub"
  RETURN
  